version: '3.3'

services:
  perception:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: perception_dev
    image: perception_image
    hostname: perception
    user: "1000:1000"  # Match host user to avoid permission issues
    working_dir: /home/dev/Documents/Autonomous-Driving-Perception-System
    stdin_open: true
    tty: true
    network_mode: "host"
    ipc: "host"
    runtime: nvidia 
    environment:
      - NVIDIA_VISIBLE_DEVICES=all  # allow all GPUs
      - CONDA_ENVS_PATH=/opt/conda/envs  # Point to the mounted environment path
    volumes:
      - /home/dev/anaconda3/envs:/opt/conda/envs  # Mount all conda environments
      - /home/dev/Documents/Autonomous-Driving-Perception-System:/home/dev/Documents/Autonomous-Driving-Perception-System
      - /home/dev/Documents/AVA_Planner:/home/dev/Documents/AVA_Planner
      - /home/dev/Documents/AVA_Control:/home/dev/Documents/AVA_Control
    command: /bin/bash
